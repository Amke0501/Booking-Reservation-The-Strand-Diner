Place the restaurant images used by the stylesheet here.

Expected filenames (as referenced in `frontend/src/styles/strand.css`):

- `The Strand Hero Page.jpg`

You can copy the screenshots you provided into this folder so the hero and reservation backgrounds load correctly.
